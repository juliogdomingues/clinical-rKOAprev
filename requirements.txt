# Runtime dependencies only. The full transitive lock will be regenerated
# from pyproject.toml with `pip-compile pyproject.toml` if/when needed.
# Versions here are the ones actually used to produce the published numbers.
numpy==2.3.5
pandas==2.3.3
scikit-learn==1.8.0
xgboost==3.1.3
scipy==1.17.0
statsmodels==0.14.6
matplotlib==3.10.8
seaborn==0.13.2
# Test/dev extras (also pinned)
pytest==8.4.2
openpyxl==3.1.5
