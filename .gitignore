# Bytecode / build
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual envs
.venv/
venv/
.env

# IDE / agent state
.idea/
.vscode/
.claude/

# OS
.DS_Store
Thumbs.db

# Restricted data (ELSA-Brasil access-controlled — never commit)
data/raw/
data/raw/*
# Participant-level audit outputs (contain idelsa IDs + per-knee outcomes) —
# regenerated on every run; must never reach the public repo.
missing_outcomes_*_ids.csv
missing_outcomes_*_rows.csv
**/missing_outcomes_*_ids.csv
**/missing_outcomes_*_rows.csv
# Belt-and-braces: never commit a workbook anywhere under data/ —
# the original "Seleção de variáveis OAJ ELSA.xlsx" had hidden sheets
# with participant-level data. Only the tidy CSV export is public.
data/**/*.xlsx
data/**/*.xls

# Test scratch
tests/fixtures/_prep_audit/
.pytest_cache/

# Local scratch sandboxes (canonical and sensitivity results/ are checked in)
results/scratch_*/
results/_*/
